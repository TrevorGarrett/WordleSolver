#include <iostream>
#include <fstream>
#include <vector>
#include <string>
#include <unordered_map>
#include <set>
#include <random>
#include "Defines.h"
#include "Functions.h"

//string solver(vector <int> result) {
//	contains[word[0]].push_back(word)
//}

void solver(Game* game) {
	std::cout << "Stuff's broke" << std::endl;
}
